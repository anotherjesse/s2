-record(object, {index,
                 bucket,
                 key,
                 size,
                 etag,
                 last_modified,
                 headers}).

-record(bucket, {index,
                 owner}).
